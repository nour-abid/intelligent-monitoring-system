# Face Embedding Microservice — Python dependencies
#
# Install into the project virtualenv (same one as the surveillance runtime):
#   pip install -r embedding-service/requirements.txt
#
# InsightFace and OpenCV are already present in the surveillance venv.
# Only FastAPI/uvicorn additions are listed separately for clarity.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0

# Already required by the surveillance runtime — listed here for explicitness:
insightface>=0.7.3
opencv-python>=4.7.0
numpy>=1.24.0
